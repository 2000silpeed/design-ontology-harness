[project]
name = "design-ontology-harness"
version = "0.1.0"
description = "Build reusable design-system knowledge bases and synthesize brand-specific system outputs."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
  "beautifulsoup4>=4.12.3",
  "httpx>=0.27.2",
]

[project.scripts]
design-ontology = "design_ontology_harness.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
