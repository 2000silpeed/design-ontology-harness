@AGENTS.md
@agent-team/TEAM_RUNBOOK.md

# Claude Code entry point

Start team work with the `design-team-lead` agent. The lead reads the shared contract, delegates the earliest incomplete stage to `.claude/agents/`, and writes the handoff ledger. Claude-specific agents must not weaken or reinterpret the shared Codex/Claude release gates.
