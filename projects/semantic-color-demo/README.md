# Semantic Color Demo

Small static demo for the imported Semantic OS color ontology package.

- Open `index.html` directly in a browser.
- The three scenarios mirror the local `build_semantic_color_context()` smoke test.
- The interface shows active palette roles, ontology matches, recommendations, and the safe palette output contract.
